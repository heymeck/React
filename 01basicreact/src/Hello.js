// It is a good practice to Capitalize the first letter of file when creating a component although it doesn't throw error.

// It is must to Capitalize the first letter of function when creating a component to render it on dom, if not
// it will not render on the page.

// this is a component function 
function Hello(){
    return (
        <h1>Hello vanilla React</h1>
    )
}

// exporting the function 
export default Hello;